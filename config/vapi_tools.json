[
  {
    "type": "function",
    "function": {
      "name": "save_patient",
      "description": "Save a new patient ONLY after the caller explicitly confirms the complete read-back.",
      "strict": false,
      "parameters": {
        "type": "object",
        "properties": {
          "first_name": {"type": "string"},
          "last_name": {"type": "string"},
          "date_of_birth": {"type": "string", "description": "MM/DD/YYYY"},
          "sex": {"type": "string", "enum": ["Male", "Female", "Other", "Decline to Answer"]},
          "phone_number": {"type": "string"},
          "email": {"type": ["string", "null"]},
          "address_line_1": {"type": "string"},
          "address_line_2": {"type": ["string", "null"]},
          "city": {"type": "string"},
          "state": {"type": "string"},
          "zip_code": {"type": "string"},
          "insurance_provider": {"type": ["string", "null"]},
          "insurance_member_id": {"type": ["string", "null"]},
          "preferred_language": {"type": ["string", "null"]},
          "emergency_contact_name": {"type": ["string", "null"]},
          "emergency_contact_phone": {"type": ["string", "null"]}
        },
        "required": ["first_name", "last_name", "date_of_birth", "sex", "phone_number", "address_line_1", "city", "state", "zip_code"],
        "additionalProperties": false
      }
    },
    "server": {"url": "https://YOUR-DOMAIN.example/vapi/tools", "timeoutSeconds": 10}
  },
  {
    "type": "function",
    "function": {
      "name": "lookup_patient_by_phone",
      "description": "Look for an existing active patient by a confirmed U.S. phone number.",
      "strict": true,
      "parameters": {
        "type": "object",
        "properties": {"phone_number": {"type": "string"}},
        "required": ["phone_number"],
        "additionalProperties": false
      }
    },
    "server": {"url": "https://YOUR-DOMAIN.example/vapi/tools", "timeoutSeconds": 10}
  },
  {
    "type": "function",
    "function": {
      "name": "update_patient",
      "description": "Update an existing patient ONLY after the caller explicitly confirms the changed information.",
      "strict": false,
      "parameters": {
        "type": "object",
        "properties": {
          "patient_id": {"type": "string"},
          "first_name": {"type": "string"},
          "last_name": {"type": "string"},
          "date_of_birth": {"type": "string", "description": "MM/DD/YYYY"},
          "sex": {"type": "string"},
          "phone_number": {"type": "string"},
          "email": {"type": "string"},
          "address_line_1": {"type": "string"},
          "address_line_2": {"type": "string"},
          "city": {"type": "string"},
          "state": {"type": "string"},
          "zip_code": {"type": "string"},
          "insurance_provider": {"type": "string"},
          "insurance_member_id": {"type": "string"},
          "preferred_language": {"type": "string"},
          "emergency_contact_name": {"type": "string"},
          "emergency_contact_phone": {"type": "string"}
        },
        "required": ["patient_id"]
      }
    },
    "server": {"url": "https://YOUR-DOMAIN.example/vapi/tools", "timeoutSeconds": 10}
  }
]
